# TODO — Петровское СП

Формат: `- [ ] задача`. Приоритет — сверху вниз. Источник правды по модели — `product-spec.md` (v1.2).

## Каталог несоответствий дизайна (причёсывание по одному экрану)

Главный экран **`screens/activities.html`** — эталон. Ниже — зафиксированные расхождения, которые сводим к канону `assets/ds.css` (раздел «эталон» в `design-system-v2.html`).

### 1. Бейдж «+N баллов» — минимум 3 визуальных варианта
| Стиль | Где | Как выглядит |
|---|---|---|
| `.st-delta` (канон, ds.css:549) | `06-home.html:101,107` | пилюля success: «+125» на зелёном |
| `.ev-pts` / `.al-pts` (локально) | `activities.html:59-60,90-91` | звёздочка `ic-star` + «+N» **акцентом**, 12px |
| `.mrow-pts` (канон, ds.css:1351) | `profile.html:167,178`, `profile-view.html:143,151`, `26-gto-result.html:139` | просто «+75» цветом `--fg`, **без звёздочки** |
| `.jr-pts` (локально) | `rating-details.html:80-81` | «+40» жирный `--fg`, `.is-neg` `--danger` |
| `.lb-pts` (локально) | `rating-details.html:131` | «+N» мелкий `--fg-2` |
| `.ms-pts` (локально) | `challenge.html:61` | «+35» `--fg-2` |
| `.pv-gto-pts` / `.p-gto-pts` (локально) | `profile.html:46`, `profile-view.html:42` | «+300» **акцентом** (знак ГТО) |
| `.ev-fact-tx` (локально) | `15-event-marathon.html:550` | «+100 баллов к цели <название>» текстом |
| Тост `toast-warn` | `activities.html:204`, `15-event-marathon.html:372`, `24-gto-workout.html:177`, `goal.html:320` | «+N баллов» в warn-тосте (эталон фикс-уведомления) |

### 2. Аватары — 3 схемы вместо одной
| Схема | Где |
|---|---|
| Фото `.avatar.is-photo` (канон) | `06-home:52`, `community:50`, `profile:88,145-148`, `profile-view:78,236` |
| Буквы на **accent** (канон, ds.css:449-467) | дефолт `.avatar-sm`, `club:248-249`, `15-event-marathon` |
| **Сплошной `--c`** + белый текст | `07-search.html:63,279-290` (`.sr-ava`) |
| **Tint 14% + oklab-текст** | `friends.html:46-53` (`.fl-ava`), `rating-details.html:120-127` (`.lb-ava`) |

### 3. Иконка-тайл «в квадратике» — 5 размеров в каноне + свои на экранах
- В ds.css уже 5 тайлов: `.n-ic` 42, `.pick-media` 48, `.evd-type-ic` 40, `.mrow-media` 44, `.row-ic` 34.
- Свои: `club.html` `.cl-ic` (56/34, дубль `.club-ic`), `03-role.html` 56px, `challenge.html` `.ch-hero-ic` (сплошной `--tone`), `07-search.html` `.sr-media` (tint) — **в одной строке поиска `.sr-media` tint и `.sr-ava` сплошной**, `profile.html:188` `.row-ic` с `color:var(--accent)` при соседних строках `fg-2`.

### 4. Имя в рейтинге/списках — 4 формата
- `rating-details.html:509-521` — полное имя `.lb-tx b` + инициалы `.lb-ava` + место `.lb-pos`.
- `15-event-marathon.html:717-727` — `.mrow.is-static` с `.r-place`, **без аватаров**.
- `club.html:239-249` — инициалы в `.avatar-sm`.
- `07-add-event.html:766,1044` и `26-gto-result.html:139` — `.mrow-ava` **без `--c`** (дефолт accent), хотя поиск ставит `--c`.

### 5. Разнобой в одном экране
- `profile.html:188` — `.row-ic` + `style="color:var(--accent)"` при соседних строках без override.

### План причёсывания (после пушa функционала)
1. Свести бейджи «+N» к одному компоненту (эталон — `activities.html`: звёздочка + accent) с тоном по контексту (success/neutral).
2. Аватары: одна схема для цветных (solid `--c`+белый ИЛИ tint — выбрать), фото-канон оставить.
3. Тайлы: канонизировать размеры, убрать сплошные заливки кроме состояний.
4. Рейтинги: единая строка (аватар + имя + место/результат).
5. Пройти все экраны по одному, сверяя с `design-system-v2.html`.

## Время тренировки (вопрос из рефлексии «где фиксируется»)

Модель перенесена в наши экраны (цепочка: таймер → факт → журнал → статы):

- `screens/24-gto-workout.html` — таймер сессии `sessNow()` (пауза копит `sessElapsed`), при завершении сохраняет `gpw:last` в localStorage (запись журнала с реальной длительностью).
- `screens/activities.html` — журнал: записи хранят `dur` (мин), мета показывает «· N мин»; подхватывает `gpw:last` первой записью.
- `screens/activities.html` — статы «Неделя» считаются от факта (часы из длительностей дней, число дней, серия).
- [x] Перенести таймер общей длительности в активную тренировку (аналог `training-active.html`) — `24-gto-workout.html`.
- [ ] Единый отчёт `training-report` со статом `Время` (`sum-dur`): экрана в `screens/` пока нет (есть только `referens/training-report.html`; `26-gto-result.html` — это сдача норматива, не тренировка). Появится при переносе экрана отчёта.
- [x] Журнал активностей: запись хранит длительность (`dur`, минуты).
- [x] Статы «Неделя» считать из фактических длительностей (не статика).

## Ренейм табов (решено, product-spec §7 / §8.1)

- [x] «События» → «Расписание», «Тренировки» → «Активности»: экраны, легенда `index.html`, таббары в `design-system.html` / `design-system-v2.html`.
- [x] Файл переименован `trainings.html` → `activities.html`, ссылки обновлены (индекс, таб-бары).

## Альфы из referens — проверка на ДС v2

- [x] `qrscan.html` и `profile-v2.html` сопоставлены с `assets/ds.css`: это референсы старой версии — использовать только как подход, не как спецификацию; QR-сканер и подтверждение участия — по §6.6/§8.1 (решение принято, реализация не начата).
- [x] QR-сканер — `screens/22-qr-scanner.html` (создан по канону ДС v2, подход `referens/qrscan.html`): сегмент «Сканер / Мой QR-код», тип кода (Клуб / Тренажёр / Друг / Мероприятие), шторка результата (вход в клуб, видеоинструкция тренажёра, профиль друга, запись на мероприятие), личный QR 21×21; проверено аудитом Playwright (14/14 PASS, без горизонтального переполнения).

## Прототипы по модели §4.20

- [x] Марафон как прототип — `screens/15-event-marathon.html`: событие-ивент → приглашение (`pending`) → подтверждение (`confirmed`) → QR-вход (`ongoing`) → опрос → запись в журнале (`done`), терминальный `cancelled`; меню «⋯» по матрице §4.17; статус карточки в календаре читается из `localStorage 'evt:marathon'`; факт попадает в журнал активностей (маркер «судья · протокол» + результат, §4.2 +50).
- [ ] Журнал активностей: маркер «как подтверждено» (я / тренер / QR / судья) — сделан только для факта марафона; распространить на обычные записи журнала.

## Уточнения активностей (referens/Activites_3.txt)

Дельта 1 (баллы + «шаг к цели» на карточках «План на сегодня») — сделана: `activities.html` строка `.ev-pts` / `.ep-goal`, `product-spec.md` §4.19/§4.20/§6.4.

- [ ] Дельта 2 — деталка события по «категория × фаза» (не только категория): завершённый ивент → карточка с участниками, местами, протоколом + у судьи кнопка «Подтвердить результат»; ивент «Открыта запись» → описание + ожидаемые баллы за участие + «Записаться». Матрица — `referens/event-details-matrix.md` (4 категории × 3 фазы: до/идёт/после).
- [x] Дельта 3 — решено: `screens/15-event-marathon.html` — универсальный `event_details` (демо-данные про марафон), не хардкод под марафон. Формат — полноэкранный просмотр с табами (паттерн SofaScore/FlashScore, `.evd*` из `design-system.html` ~1946–2076): шапка-герой (название + категория-чип + статус-пилюля) → таббар → тело со скроллом → нижняя CTA-плашка. Попапом НЕ обойтись — это полноценный турнир/событие. Состав табов зависит от данных события (не от типа экрана): `bracket_available` → «Сетка», фаза/результаты → «Результаты», участники → «Участники». Марафон (забег, сетки нет) → табы **Обзор / Результаты / Участники**; турнир-пример («Летний Кубок», теннис) → табы **Обзор / Сетка / Результаты / Участники** с бракетом `.evd-brk*` и пьедесталом `.evd-podium`. Унификация: один `event_details`, типы ивента (дуэль / чемпионат / марафон / заплыв) различаются только данными и форматом строк результата (счёт / время / место), не структурой экрана. Дуэль = бракет из одного финала; чемпионат = бракет + группы, но те же `rounds/matches` (ТЗ: `GET /api/mobile/events/{id}/bracket` → `rounds` + `match {score1, score2, status, winner}`, тз.md:1273–1306). Зафиксировано в `product-spec.md` §6.4 (`event_details`).
- [x] Фотоотчёт завершённого ивента — НЕ галерея на ивенте (в модели event в ТЗ фото нет, сущность не плодим): в фазе «После» в Обзоре — **ссылка на новость сообщества** (там уже фото/видео и разбор «как прошло»). Галерея/видео/подробный отчёт — контент новости, а не деталки ивента.
- [x] Судья — структура заложена в матрице (`event-details-matrix.md` «Судейский блок»): «Подтвердить результат» по каждому участнику в фазе идёт/после, только для назначенного судьи события; баллы за 1/2/3 место (§4.2). В `event_details` кнопку судьи не рендерим, пока роли нет — точка входа добавлена в план судейского флоу (§8.4).
- [x] Дельта 2 (реализация) — табы собраны в `screens/15-event-marathon.html`: перенесены `.evd*`-стили из ДС v1 (~1946–2076) локально в `<style>`, переключение табов на JS `setTab()` (`.is-on` + `aria-selected`, не CSS `:checked`). Марафон → табы **Обзор / Результаты / Участники** (сетки нет — забег); турнир-пример «Летний Кубок» (`?ev=cup`) → таб **Сетка** добавляется в рантайме (бракет `.evd-brk*` 4 колонки + горизонтальный скролл, пьедестал `.evd-podium` 3 места, участники с тегами «Победитель/Финалист», CTA «Читать новость»). `flow-test.js` обновлён под табы (check 5 «Кто идёт» → переключение на таб «Участники» вместо `#who-sheet`; +checks 28–34 для Кубка) — **35/35 PASS**. По пути починен гэп ДС: `.topbar-act svg` не имел размера в `assets/ds.css` (иконка раздувалась до 300px, горизонтальный оверфлоу тела 346px) — локальное правило `width/height: var(--icon-md)`.
- [x] Деталка события переведена на ДС v2-канон (вместо ДС v1-языка, из которого табы переносились): шапка действий — `.icon-btn` (48px, `ds.css` 381–399) вместо `.topbar-act` (гэп с размером `svg` исправлен заменой, а не костылём); списки участников/результатов — `.mrow`-семейство (`ds.css` 1268–1320: `.mrow-ava` 44px, `.mrow-t/.mrow-s`, `.mrow-side .tag`) вместо локальных `.evd-part*`/`.ev-ava`; аватары — `.avatar`/`.avatar-sm` (буквы на `--accent`, `ds.css` 452–469) вместо буквенных цветных `.ev-ava` ДС v1. JS: `avatar(name, sm, cls)` переписан на `initials(name)`; локальные `.evd-part*`, `.ev-ava`, `.topbar-act svg` из `<style>` удалены. `flow-test.js` селекторы обновлены (`.evd-part-row` → `.mrow`) — **35/35 PASS**, `evd-audit.js`: `hOverflow` = 0 на всех табах (сетка — намеренный внутренний скролл 703/390px).
- [x] «Строка пользователя» унифицирована с `.mrow` (решено: одна сущность вместо двух стилей одной таблицы): `.user-row*` удалён из `assets/ds.css` и `07-add-event.html` (`renderFriendsList`/`renderPeopleList` → `.mrow/.mrow-ava/.mrow-t/.mrow-s/.mrow-check` + `.is-on`), аватары объединены в одну базу `.avatar, .avatar-sm, .ava-72, .mrow-ava`. `user-row` в проекте не осталось. По пути: `#evd-bar` аренды не имел id → `$("evd-bar").offsetHeight` падал (`null`) при выборе площадки — добавлен `id="evd-bar"`. Смоук `rental-rows.js`: 10 строк, выбор ставит `is-on`+чек, друзья 1 строка; регрессия чистая (35/35, hOverflow=0).
- [x] Дельта 4 — прогресс цели на карточках: `goal:active` хранит `{title, sign, deadline}` + счётную метрику `metric: {label, done, total, next}` (пишет `screens/goal.html`: ГТО — нормативы из «Прогресса нормативов», Регулярность — занятия по плану); `screens/activities.html` читает: кап «Цель: … · 4/5», маркер `.ev-goal` — пилюля `ic-target` + «4/5», формула «сделаешь — станет 5 из 5» в `aria-label`.

## Редизайн «Активности» (стрик → план + лента)

- [x] Убран дашборд-стрик («3 дня подряд», «лучшая серия», «3,5ч · 3 дня · 4 активности») — неинформативные метрики, дублировали журнал.
- [x] Цель стала частью «Плана на сегодня» (`trn-goal` в `activities.html`, читает `goal:active`).
- [x] Журнал переделан в ленту `.al-row`: когда · что делал · что дало (результат + баллы), сумма баллов в шапке `.sec-count`. Единая хронология без групп по неделям.
- [x] Период журнала — сегмент `.seg` Неделя/Месяц/Всё (фильтр по метке `at`, сумма за период в `.sec-count`).
- [x] Лента кодирует категорию бледной цветной полосой слева (`--c`, тот же язык, что яркая полоса на карточках «Сегодня», но приглушённый — 42% насыщенности).
- [x] «Шаг к цели» — не подпись, а маркер `ic-target` в **левом** верхнем углу карточки «Сегодня`: карточка объявляет цель (`t.goal = 'Сдать нормы ГТО'`), маркер виден только если она совпадает с `goal:active.title` (смена цели сверху убирает маркер с чужих карточек); тап по маркеру → экран цели (goal.html), не в деталку тренировки.
- [x] Баллы как мотивация — в деталке события до/во время решения (`15-event-marathon.html`): обещание «Участие принесёт +50 баллов · значок» на стадии приглашения и подтверждения (ранее были только по итогу). На карточках главной/календаря баллы НЕ показываем — там навигация, баллы = шум (решение: деталка, не везде).
- [x] Дневник посещаемости из ТЗ (2.3.10 «Моя посещаемость», тз.md:479) — **сделан** в `rating-details.html` (вкладка «Посещаемость», §6.6): hero «N посещений» (+2 к прошлому месяцу), календарь-heatmap (42 ячейки августа 2026, 8 посещений по `HM_MAP`, уровни l1–l4, рамка «сегодня», легенда «Меньше/Больше», навигация ◀ ▶), график «По месяцам» (6 столбиков `HM_STATS`), «Экспорт в Excel» (тост). Модель `GET /api/mobile/attendance` (тз.md:1025) → `HM_MAP` (день→кол-во) + `HM_STATS`; месяцы вне демо-августа — детерминированная псевдокарта. Попутно починено переполнение журнала начислений: `.jr` (grid-элемент) не имел `min-width: 0` — строки вылезали на 396px при 390px-вьюпорте. `package-audit.js` расширен (блок `att` + «journal rows fit viewport») — **74/74 PASS**.

## Пакет «Профиль и сервис» (§6.6)

- [x] `screens/profile.html` — свой профиль (таб «Профиль» в таб-баре): шапка-герой (фото, ФИО, роль · возраст · ID), плитки «Баллы · уровень / Серия / Мой QR» + место в регионе, карточка знака ГТО (серебро, `assets/gto/sign-silver.png`) → goal.html, лента друзей → friends.html, активности (+75/+40), меню (План тренировок / Настройки / Выйти — тост-заглушки). Канон: `.hd`, `.dash-stats`, `.dash-card`, `.stat-rating`, `.gto-badge`, `.sec/.sec-head`, `.evd-rows`, `.row`, `.tabbar`.
- [x] `screens/rating-details.html` — баллы и рейтинг (вход — из профиля): сегменты Баллы/Рейтинг; уровень «Активный» 1 280 → «Мастер», бар со шкалой 0/1000/2500; серия 6 дней; журнал начислений §4.2 со статусами подтверждено/ожидает/отклонено/система (+40 зал, +20 самост., +75 место, +30 аренда, +300 знак, +10 серия, −50 штраф); лидерборд по скоупам Регион/Друзья/Сообщество (место-карточка «12 · +2»); лист «Правила начислений» (таблица §4.2) — `.seg`, `.fchip`, `.scrim/.sheet`.
- [x] `screens/profile-view.html` — чужой профиль (`?id=p1..p6`, вход — поиск и друзья): герой + чипы (Друг/ГТО/Клуб), действия «Добавить в друзья» (переход в «Заявка отправлена»)/«Написать», статы (баллы/серия/друзья), знак ГТО с прогрессом, общие данные, недавние активности.
- [x] `screens/friends.html` — друзья: живой поиск по имени, скоуп-чипы Все(24)/Онлайн(6)/Заявки(2), строки → profile-view, заявки с «Принять»/«Отклонить» (удаляют строку и правят счётчик), пустое состояние.
- [x] `screens/challenge.html` — деталка челленджа (`?id=ch1|ch2`, вход — поиск): герой-карточка (иконка, участники, «осталось дней», онлайн), прогресс-бар (21/30 дней · 64/100 км), баллы за этапы (done/next/lock), «Поделиться прогрессом».
- [x] Входы связаны: таб «Профиль» в `06-home.html`/`07-calendar.html`/`activities.html` → `profile.html`; люди в `07-search.html` → `profile-view.html?id=…`, челленджи → `challenge.html?id=…` (клубы остались тост-заглушкой). `index.html` (секция «Профиль и сервис») и `product-spec.md` §6.2/§6.6 обновлены.
- [x] `screens/profile-edit.html` — редактирование профиля (вход — иконка ✎ в шапке `profile.html`, вместо тост-заглушки): фото (6 пресетов `assets/avatars/*` + «Без фото» → инициалы, нижний шит), ФИО, дата рождения (барабан, как анкета), стаж, пол, виды спорта (чипы, ≥1), город, телефон (маска +7), email, «о себе», родитель/доверительное лицо (блок виден только если возраст <18); валидация и маски — канон `05-questionnaire.html`; «Сохранить» (`.evd-bar`, disabled до валидности) пишет `localStorage['user:profile']` → тост → `profile.html`. Чтение `user:profile` добавлено в `profile.html` (имя/роль·возраст·ID/фото) и `06-home.html` (приветствие по имени + аватар). `index.html` §06.6 и `product-spec.md` §6.6 обновлены. Аудит `pe-audit.js` — **26/26 PASS**: загрузка, дефолты, hOverflow=0, wheel-дата, фото-шит (вкл/выкл/инициалы), валидация→save→переход, read-back в profile/home, персистентность reload, блок родителя (взрослый/ребёнок), без console errors. Регрессии: `package-audit.js` **74/74**, `community-audit.js` **60/60**, `states-audit.js` **29/29 PASS**.

## Пакет «Сообщество» (Фаза 0 + 1, §6.5)

- [x] Фаза 0 — дефолты в `product-spec.md` §8: §8.3 чат не переносится (кнопка «Написать» — статичный тост, консультации вне прототипа); §8.7 дневник посещаемости → Профиль (heatmap, `rating-details.html`), награды = знаки ГТО + баллы (§4.2); §8.10 тренер = подвид `profile-view`; §8.12 свои события — ⋯-меню в календаре → мастер `07-add-event` с предзаполнением. Открыты, не блокируют: §8.4 (одобрение судейства), §8.11 (файл из ТЗ).
- [x] `screens/community.html` — лендинг таба «Сообщество»: `.hd` (аватар, поиск, уведомления `.notif`), сегмент Лента/Клубы (`.seg-btn[data-panel]` + `.cm-panel` hidden), лента 7 новостей → `news.html?id=n1..n7`, сетка 9 клубов → `club.html?id=c1..c9`, «Создать клуб» — тост, «Выйти»; таб «Сообщество» активен.
- [x] `screens/news.html` — деталка новости (`?id=n1..n7`): герой (иконка/лейбл/градиент, фото-ассетов нет), дата + теги `.tag`, тело, «Нравится» (`.is-liked` + счётчик + тост), «Поделиться» (тост), «По теме» — относительные ссылки (клуб/событие/рейтинг), back → community.html, таб-бар.
- [x] `screens/club.html` — деталка клуба (`?id=c1..c9`): hero-карточка, «Вступить» (`.btn-accent`→`.btn-success` +1 участник + тост), «О клубе», «Новости клуба» → news, «Участники» (`.avatar-sm` → profile-view), «Тренер» (+«Написать»-тост), «Расписание» → `07-calendar.html`.
- [x] Входы связаны: клубы в `07-search.html` → `club.html?id=c1..c3` (тост-заглушка убрана); `06-home.html` — клубы → `club.html?id=c4..c9`, новости → `news.html?id=n2/n3/n4`, «Все» → community.html, таб «Сообщество» → community.html; `07-calendar.html` таб → community.html; таб-бары `profile/friends/profile-view/challenge/activities/15-event-marathon` → `community.html`; CTA «Читать новость» в `15-event-marathon.html` (3 обработчика: меню, bar, stage) → `news.html?id=n1`. `index.html` §05 и `product-spec.md` §6.5 обновлены.
- [x] Аудит `community-audit.js` — **60/60 PASS**: community (seg Лента/Клубы, 7 новостей, 9 клубов, тосты), news?id=n1/n6 (лайк 47→48, related: клуб+событие / рейтинг), club?id=c4/c5/c2 (join 41→42, участники/новости/расписание/тренер, участник p8), profile-view?id=p8, поиск клубов (c1/c2), home-ссылки (c4..c9, n2..n4), таббары, CTA события. По пути: `.mrow` получил `min-width: 0` в `assets/ds.css` (длинные названия в `.evd-rows` вылезали на 404px — переполнение), `profile-view.html` PERSON расширен до p8/p9/p11/p13/p14/p15 (иначе участники клубов фолбэком падали на p1), `club.html` PEOPLE получили фото. `flow-test.js` обновлён под новое поведение CTA (переход на news.html?id=n1 вместо тоста) и под метрику капа цели (« · 4/5») — **35/35 PASS**; `package-audit.js` — **61/61 PASS** (регрессия после правки ds.css).

## Точки на доработку

- [x] Категория «Тренировка ГТО» в журнале: оставлен `--gto-silver` как метка медали норматива (решение принято; teal не применяется).
- [ ] Челленджи: слой вовлечения вне расписания — карточка прогресса в ленте/профиле (после фундамента). Деталка `screens/challenge.html` уже собрана (вход — поиск).

## Инвентаризация подвешенных экранов (запрос пользователя 2026-08-17)

Роли **тренера и судьи отложены «пока что»** — Фаза 4 не начинается (09-club-schedule не создавался).

Прочесаны `07-add-event.html`, `05-questionnaire.html`, `06-home.html`, `activities.html`, `goal.html`, `ds.css`, `referens/create-event.html`, `24-gto-workout.html`. Итоги:

- [x] **Экран успеха после анкеты** — сделан в `05-questionnaire.html`: submit → `#q-done` (паттерн step-done референса: `.done-ic`-чек + «Анкета отправлена!» + `.done-sub` + карточка с ФИО из `qFio` + заметки «Анкета сохранена»/«Совет: добавьте цель») → кнопка «На главную» → `06-home.html`. В спрайт добавлены `ic-check`/`ic-user`/`ic-target`. Back/Escape после успеха → `06-home.html` (вместо `03-role.html`). Проверено `q-done-check.js` (13/14, единственный FAIL — пре-существующий 404 favicon сервера).
- [x] **Пустое состояние «нет активной цели»** — сделан в `06-home.html`: goal-card без `goal:active` становится `.is-empty` («Цель не выбрана», `ic-target`, CTA «Выбрать цель», ссылка → `goal.html`, `aria-label` обновлён, бар прогресса убран). Модель: `activeGoal()`/`GOAL_SIGN_TOKEN` как в `activities.html`.
- [x] **Дашборд читает цель из `goal:active`** — сделан в `06-home.html`: карточка динамическая — заголовок из `g.title`, чип знака из `g.sign` (токен `--gto-*`), прогресс/счёт из `g.metric` (`pct = round(done/total*100)`, «N/M label», `aria-label`); своя цель без метрики — бар и счёт скрыты, в подписи дедлайн. Проверено `goal-card-check.js` (3 состояния: нет цели / ГТО 4/5 → 80% / своя) — **24/24 PASS**, hOverflow=0.
- [x] **Экран успеха аренды** — закрыт пунктом 5: `07-add-event.html` теперь аренда идёт `facility → extras → confirm → done` (done = паттерн step-done референса, «Бронь площадки подтверждена»), `finish()` больше не делает тост+редирект.
- [x] **Тренировка/клубное событие — полный флоу** (`07-add-event.html`): не-аренда теперь `details → (trainer если формат «С тренером») → confirm → done`. Добавлены шаги `step-trainer` (тренеры `TRAINERS` + слоты + длительность 1/2 ч, `renderTrainer`), `step-confirm` (сводка `.summary`/`.sum-row`/`.type-pill` через `confirmRows()`, подпись по типу) и `step-done` (`renderDone`: статус-плашка, дата/место/цена/участники). Степпер точек теперь общий `stepsFor(t)/contentSteps()` для всех типов (3 точки тренер/аренда, 2 — личная тренировка/клуб). Кнопки: details «Далее» → trainer (если «С тренером») или confirm; trainer/facility/extras «Далее» → дальше по шагам; confirm «Забронировать»/«Отправить заявку»/«Создать событие» → done; done «Готово» → `07-calendar.html`. `updateRecap()` расширена под тренера. В `assets/ds.css` добавлены переиспользуемые `.summary/.sum-*/.type-pill/.done-*/.done-status`. Проверено `evd-flow-check.js` (оба флоу, клуб, личная) + hOverflow=0, регрессия `package-audit.js` **74/74 PASS**.
- [x] **Общего диалога подтверждения да/нет нет** — закрыто без изменений: канон уже полностью в `assets/ds.css` (`.scrim`/`.sheet`/`.sheet-grab`/`.sheet-head`/`.sheet-title`/`.sheet-close`/`.sheet-primary`/`.sheet-cancel`, плюс `.sheet-note` в `ds.css:1948` как текст подтверждения). Эталонные примеры — `24-gto-workout.html` (161-172, 459-468) и `07-notifications.html` (129-139). Дубликат `.gpw-confirm-sub` — локальный стиль экрана, в канон не вводился.
- [ ] **Список людей в «Сообществе» — НЕ делаем** (согласовано): в `community.html` только Лента/Клубы; люди — через поиск `07-search.html` (категория «Люди»), друзей, лидерборд, клубы.

## Открытые вопросы — в `product-spec.md` §8 (не дублировать здесь)

- Роль судьи в интерфейсе (§8.4), состав переносимых экранов (§8.6), файл-приложение к §8.11.

## Карта готовности экранов (по `index.html` §01–§08)

| Раздел | Готово / всего | Не собрано |
|---|---|---|
| 01 Вход и онбординг | 7 / 8 | 04-onboarding |
| 02 Главная | 6 / 6 | — |
| 03 Расписание и запись | 0 / 2 | 08-schedule, 09-club-schedule |
| 04 Календарь и тренировки | 3 / 7 | 10-calendar, 11-bookings-calendar, 12-bookings-list, 14-post-training-survey |
| 05 Сообщество | 3 / 3 | — |
| 06 Профиль и сервис | 7 / 7 | — |
| 07 ГТО и тренировки · перенос | 5 / 9 | 27-training-active, 28-training-report, 29-schedule-calendar, 30-training-journal |
| 08 Состояния | 4 / 4 | — |
| **Итого** | **35 / 50 (~70%)** | 15 |

- Физически в `screens/` 33 файла (включая 4 в `screens/states/`): марафон и турнир — один файл `15-event-marathon.html` (`?ev=cup`).
- Часть «плановых» пунктов — кандидаты на вычёркивание по архитектуре `product-spec` §6: 10–12 устарели (заменены `07-calendar`/`07-search`), 08–09 частично закрываются табами `07-calendar`, 27–30 — `activities.html` + `24-gto-workout`.
- Фаза 2 (конструктор тренировок) и Фаза 5 (состояния S1–S4 + §9 чек-лист + регрессия) — закрыты; остаток закрывают Фаза 3 (мелкий пакет профиля) и Фаза 4 (тренер/судья → 09, §8.4).

## Фаза 2 — Конструктор тренировок (§4.16, §4.19, §6.6)

- [x] Изучена модель: §4.16 (единый шаблон = название + тег-цель + сетка упражнений по дням + «сгенерировать N тренировок»), §4.19 (цель = пресет + метрика + план + дедлайн), §6.6 (`training_constructor` — раздел профиля), референсы `referens/gto-plan.html`/`23-gto-plan.html`.
- [x] `screens/13-training-constructor.html` — «План тренировок»: табы-пресеты **ГТО** (Подтягивания, 3 недели × 7 дней) / **Общая форма** (2 недели) / **Своя** (1 неделя, редактируемое название); карточка (тег-цель, период, кол-во упражнений), «Из опросов» — рекомендация с кнопкой «Добавить»; сетка упражнений × дни (включить/исключить ячейку, × — убрать упражнение, «+ Добавить упражнение» из пула); степпер «Сгенерировать N тренировок»; «Пересадить в календарь» → тост + ссылка «Открыть расписание» + `localStorage 'plan:active'`; «Начать тренировку» (ГТО → `24-gto-workout`, остальные — тост-подсказка). Каркас и JS-паттерны — как в `23-gto-plan.html`.
- [x] Вход из `screens/profile.html`: «План тренировок» → `13-training-constructor.html` (ссылка вместо тост-заглушки).
- [x] `index.html` §04 (тег «Готово») и `product-spec.md` §6.6 (`training_constructor` → `screens/13-training-constructor.html`) обновлены.
- [x] Аудит `constructor-audit.js` — **29/29 PASS**: рендер 3 пресетов, сетка 4×7 (h-скролл), сегмент недель, переключение пресетов, переименование «Своей», выключение ячейки (9→8), добавить/удалить упражнение, рекомендация (Добавить → Добавлено ✓), степпер (3→4), выгрузка (тост + ссылка + `plan:active` + кнопка заблокирована), «Начать тренировку» по пресетам, профиль → конструктор, hOverflow=0. Регрессии: `flow-test.js` 35/35, `package-audit.js` 61/61, `community-audit.js` 60/60.
- [x] Починена сетка таблицы конструктора (не было `grid-template-columns` — все ячейки складывались в один столбец 738px, «День N» не в своих колонках): задано `grid-template-columns: minmax(150px,1.6fr) repeat(var(--tc-days), minmax(84px,1fr))`, `--tc-days` ставится в рантайме (7/7/5), длинные значения переносятся (как канон `referens/gto-plan.html`). Аудит расширен (checks 6.1–6.4: число колонок, заголовки День 1..7, отсутствие переполнения) — **33/33 PASS**.

## Фаза 5 — Состояния S1–S4 (§08, index) + §9 чек-лист + регрессия

- [x] **S1 Пусто** — `screens/states/empty.html`: три контекста **Лента / Календарь / Журнал** через `.seg` (по desc в index §08), каждый — `.empty` (иконка-тайл + «что случилось» + «что делать» + CTA на реальный экран: `06-home.html`, `07-search.html`, `activities.html`).
- [x] **S2 Офлайн** — `screens/states/offline.html`: баннер `.off-banner` (warn-тон, `ic-wifi-off`, «Нет соединения · показываем сохранённые данные») + 2 закэшированные карточки `.n-card` (мета «Сохранено офлайн · 12:05») + подпись «Кэш обновлён …» + «Повторить попытку» (`.btn.is-busy` → «Снова в сети», баннер `.is-online`, тост).
- [x] **S3 Загрузка** — `screens/states/loading.html`: скелетоны `.skel-card`/`.skel-line` (габариты повторяют `.n-card`, ничего не прыгает) → через 1.4 с контент появляется кадром `od-enter` (`od-reveal`).
- [x] **S4 Ошибка** — `screens/states/error.html`: полноэкранное `.state` (иконка-тайл `.state-ic.is-danger` + «Что-то пошло не так» + пояснение) + «Повторить» (`is-busy` → контент `od-enter` + тост «Данные обновлены») + «На главную» (`06-home.html`).
- [x] Компоненты в `assets/ds.css` ЭТАП 10: `.state`/`.state-ic` (is-muted/is-accent/is-warn/is-danger)/`.state-title`/`.state-sub`/`.state-actions`, `.off-banner` (+`.is-online`), `.skel-card`; `[hidden]`-перебивка расширена на `.empty`/`.state`/`.n-card`/`.off-banner`. Иконки `ic-wifi-off`/`ic-alert`/`ic-refresh` добавлены в `assets/ds-icons.svg` (23 символа), `design-system-v2.html` §описание спрайта обновлён.
- [x] `index.html` §08 — 4 экрана с тегом «Готово»; `product-spec.md` §9 чек-лист приведён к факту (проставлены `[x]` для собранных экранов: welcome/auth/role/questionnaire/home/07-calendar/меню события/конструктор/лента/сообщества/профиль/QR + новый пункт «Состояния S1–S4»).
- [x] Аудит `states-audit.js` — **29/29 PASS**: 4 экрана, hOverflow=0, контексты S1 (сег-переключение), флоу S2 (retry → снова в сети), S3 (скелетон → контент od-enter), S4 (retry → контент + тост), без console errors. Регрессии: `package-audit.js` **74/74 PASS**, `community-audit.js` **60/60 PASS**.
- [x] Попутно: выяснилось, что `fonts.googleapis.com` из этой сети недоступен (`net::ERR_TIMED_OUT`) — стили шрифтов висят до 30 с и задерживают `domcontentloaded`; аудиты состояний ждут `commit` + селектор (не `domcontentloaded`), сами экраны деградируют на системный шрифт.

## Сводка для перезапуска диалога

Обновлено: 2026-08-17. Это компактный слепок состояния, чтобы восстановить контекст.

### Главная тема (последняя итерация)
Доделывание функционала по пакетам: **редактирование профиля** (`screens/profile-edit.html`) собрано и связано — имя/возраст/роль/фото в `profile.html` и приветствие/аватар в `06-home.html` читаются из `localStorage['user:profile']`. Проверено `pe-audit.js` **26/26 PASS**; регрессии `package-audit.js` **74/74 PASS**, `community-audit.js` **60/60 PASS**, `states-audit.js` **29/29 PASS**. Следующие шаги после пуша: причёсывание дизайна по одному экрану (каталог несоответствий вверху) + утверждение ДС, экран «Спорткомплекс» для карточки «СП Петровское» (решение принято: Экран Комплекс + схема, §27–28 ДС).

**Реализовано (эта итерация):**
- `screens/profile-edit.html` — экран редактирования: фото (6 пресетов + «Без фото» → инициалы), ФИО, дата рождения (wheel, WB 6–99), стаж, пол, виды спорта (чипы), город, телефон (маска), email, «о себе», блок «Родитель» (виден только <18). Валидация — канон анкеты (`05-questionnaire.html`), сохранение — `user:profile` в localStorage, CTA `.evd-bar` disabled до валидности.
- `profile.html` — кнопка ✎ теперь `href="profile-edit.html"` (была тост-заглушка); JS подставляет из `user:profile` имя, «Спортсмен/Спортсменка · N лет · ID 4581» (возраст считается от даты рождения), фото/инициалы.
- `06-home.html` — «Привет, <имя>» и аватар из `user:profile` (фолбэк — демо-Андрей).
- Доки: `index.html` §06.6 (+profile-edit), `product-spec.md` §6.6 (+строка `profile_edit`), TODO.md карта 06 → 7/7, итого 35/50.

**Реализовано:**
- `rating-details.html` — вкладка «Посещаемость»: hero «8 посещений» (+2 к июлю), календарь-heatmap 42 ячейки (август 2026, `HM_MAP`, ровно 8 посещений как в ТЗ, уровни l1–l4, рамка «сегодня», легенда), навигация ◀ ▶ по месяцам (июль — 35 ячеек), столбиковый график «По месяцам» (6 месяцев), «Экспорт в Excel» → тост. API `GET /api/mobile/attendance` → `HM_MAP` + `HM_STATS`, прочие месяцы — детерминированная псевдокарта.
- Попутно починен баг журнала начислений: `.jr`-строки вылезали за 390px-вьюпорт (R=396) — добавлено `min-width: 0` (grid-элемент). Замер hOverflow в аудитах на `domcontentloaded` не ловил это (шрифты не загружены) — добавлена проверка «journal rows fit viewport».
- `package-audit.js` расширен блоком `att` (ячейки/сегодня/уровни/8 посещений/навигация/столбики/экспорт) — **74/74 PASS**; регрессии: `constructor-audit.js` **33/33 PASS**, `community-audit.js` **60/60 PASS**.

**Предыдущая тема:** дневник посещаемости (heatmap, ТЗ §2.3.10) — собран в `screens/rating-details.html` (третья вкладка «Посещаемость» в разделе статистики профиля) и проверен: `package-audit.js` **74/74 PASS**.

**Реализовано:**
- `community.html` — таб «Сообщество»: сегмент Лента/Клубы, лента 7 новостей, сетка 9 клубов, «Создать клуб»-тост.
- `news.html` (`?id=n1..n7`) — деталка новости: лайк/поделиться-тосты, «По теме»-ссылки.
- `club.html` (`?id=c1..c9`) — деталка клуба: «Вступить» (+1 участник), новости, участники, тренер, расписание.
- Входы: поиск (клубы c1..c3), home (клубы c4..c9, новости n2..n4, «Все»), таб-бар всех экранов → community.html, CTA события → news.html?id=n1. §8.3/§8.7/§8.10/§8.12 закрыты дефолтами. Попутно: `.mrow {min-width:0}` (переполнение в `.evd-rows`), PERSON profile-view расширен до p8+.

**Следующая тема — линковка экранов (по запросу пользователя).** Инвентаризация `screens/` (28 файлов) через `nav-audit.js` (матрица достижимости + битые `.html`-ссылки): починен QR-сканер (кнопка назад вела в несуществующий `index.html` → `history.back()/profile.html`) и `26-gto-result.html` (`18-profile.html` → `profile.html`); `00-splash.html` — добавлен автопереход `01-welcome.html` (был без входа); `02-pin.html` — «Войти по ПИН» из `02-auth.html`. Главная `06-home.html` дозалинкована: «Ближ. События → Все» (`07-calendar.html` вместо `href="#"`), ev-карточки кликабельны (`.is-link`, тап → календарь, «⋯» по-прежнему открывает шит), промо-баннер — ссылка на `15-event-marathon.html`/`07-calendar.html` (поле `to` в `BANNERS`), стат-карточки «Баллы/Достижения/Рейтинг» — `<a href="rating-details.html">` (как в profile). Все 28 экранов достижимы, битых ссылок нет (`nav-audit.js` — 0 BROKEN). Регрессии после правок: constructor-audit **29/29**, flow-test **35/35**, package-audit **61/61**, community-audit **60/60 PASS**; тап по ev-карточке/меню, промо и стат-карточкам проверены отдельным Playwright-сценарием (все переходы работают, шит не конфликтует).

**Предыдущая тема:** пакет «Профиль и сервис» (`profile.html` → `rating-details.html` → `profile-view.html` → `friends.html` → `challenge.html`), аудит `package-audit.js` **61/61 PASS**, hOverflow=0 на всех пяти экранах; попутно починен дисбаланс данных (онлайн-друзей 5 при счётчике 6 → Лена Шувалова в онлайн). Входы: люди → `profile-view.html?id=…`, челленджи → `challenge.html?id=…` из поиска; таб «Профиль» в home/calendar/activities. `index.html` §06 и `product-spec.md` §6.2/§6.6 обновлены.

**Предыдущая тема:** универсальная деталка события `event_details` (`screens/15-event-marathon.html`, §6.4): табы Обзор/Результаты/Участники (+Сетка для Кубка), фазы pending→…→done, степпер, опрос, факт → журнал; аренда `07-add-event.html` на `.mrow`-языке; `flow-test.js` 35/35 PASS.

**Тесты:** `C:\Users\Rakutin\AppData\Local\Temp\opencode\package-audit.js` — **74/74 PASS** (пакет «Профиль и сервис» + дневник посещаемости); `pe-audit.js` — **26/26 PASS** (редактирование профиля + связка user:profile); `constructor-audit.js` — **33/33 PASS** (Фаза 2, конструктор тренировок); `flow-test.js` — **35/35 PASS** (деталка события); `community-audit.js` — **60/60 PASS** (пакет «Сообщество»); `states-audit.js` — **29/29 PASS** (Фаза 5, состояния S1–S4; `waitUntil:'commit'` из-за недоступных Google Fonts). Сервер http://127.0.0.1:8765/ (нужно запускать перед тестом). Харнесс: playwright-core, `channel:'msedge'`, 390×844.

### Среда
- ОС win32, PowerShell 5.1, **`rg` НЕ доступен** — только `grep`/`Get-ChildItem`/Select-String. Модель не читает изображения (скриншоты для проверки не использовать — только DOM-метрики через playwright `$$eval`).
- Временная зона харнесса: `C:\Users\Rakutin\AppData\Local\Temp\opencode\` (playwright-core, скрипты `flow-test.js`, `evd-*.js`).
- Файлы проекта в корне: `design-system.html` (ДС v1), `design-system-v2.html` (ДС v2), `product-spec.md` (модель, v1.2), `tz.md` (ТЗ), `TODO.md`, `referens/` (матрицы, старые экраны), `screens/` (актуальные экраны), `assets/ds.css` (общий CSS).

### Как работает марафон (схема данных)
- `localStorage 'evt:marathon'` = `{status, q1, q2, fact}`; `fact` = запись для журнала (маркер «судья · протокол», результат «3:42:11 · 124-е место из 487»).
- Календарь `07-calendar.html` читает статус из storage для карточки 30 авг; `activities.html` — подхватывает факт первой записью ленты.
- `geo-act.js` (в temp) подкладывает `evt:marathon {status:'done'}` и читает `#trn-logs .ev-card` — не затрагивался.

### Осталось (по TODO выше)
1. **Подвешенные экраны — ВСЁ ЗАКРЫТО** (июль-август, см. раздел выше): экран успеха анкеты, пустое состояние «нет активной цели» + чтение `goal:active` на дашборде, экран успеха аренды и полный флоу тренировки/клубного события (пункт 5 закрыл пункт 4), редактирование профиля (`profile-edit.html`, **26/26**). Регрессия `package-audit.js` **74/74**, `community-audit.js` **60/60**, `states-audit.js` **29/29**, `goal-card-check.js` **24/24**, `q-done-check.js` **13/14** (FAIL = пре-существующий 404 favicon), `evd-flow-check.js` OK.
2. **Фаза 3** — мелкий пакет профиля: 07-notifications, 07-search, 07-add-event собраны и входят в карту (линковка закрыта), но визуальная проверка глазами не завершена; «Дневник посещаемости» (heatmap, §2.3.10) — отложен в профиль.
3. **Фаза 4 — тренер/судья — ОТЛОЖЕНА «пока что»** (решение пользователя): 09-club-schedule (§8.4) и связанные флоу не начинать.
4. **Фаза 5 — ЗАКРЫТА**: состояния S1–S4 (`screens/states/*.html`, `states-audit.js` **29/29 PASS**), §9 чек-лист приведён к факту, итоговая регрессия `package-audit.js` **74/74** + `community-audit.js` **60/60 PASS**.
5. Маркер «как подтверждено» (я/тренер/QR/судья) — распространить на обычные записи журнала (сейчас только марафон).
6. Челленджи — карточка прогресса в ленте/профиле (деталка `challenge.html` собрана, вход — поиск).
7. Открытые вопросы в product-spec §8.4/§8.6/§8.11.
8. Визуальная проверка глазами пользователя — пакеты «Профиль и сервис», «Сообщество», конструктор, состояния S1–S4 после сборки.
9. **После пуша:** причёсывание дизайна по одному экрану (каталог несоответствий вверху), утверждение ДС; экран «Спорткомплекс» для карточки «СП Петровское» (`06-home.html:61`, решение: Экран Комплекс + схема поселения, канон — ДС §27–28, компоненты `.zone-card`/`.scheme`/`.feat-row` в ds.css v2 ещё не перенесены).
