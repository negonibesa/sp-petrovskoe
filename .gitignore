# Служебные папки Open Design (не часть прототипа)
.file-versions/
.od-skills/
*.artifact.json

# Внутренние референсы для разработки (не часть приложения)
referens/

# Генерируемые .fig.json для выгрузки в Figma (пересоздаются build.mjs)
figma-export/
