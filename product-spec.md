# Спортивная платформа «Петровское СП» — канон продукта

> **Это единственный источник правды по продукту.** Все правки логики, ролей, сценариев и карты экранов — здесь, в этом файле. Остальные документы на него ссылаются.

**Версия:** 1.0
**Актуально для:** рефакторинга дизайн-системы v2 и переноса экранов из старой ДС.

---

## 0. Связанные файлы

| Файл | Роль |
|---|---|
| `Информационно-функциональная-архитектура.JSON` | Актуальный флоу (экраны, связи, ключевые сценарии). Мастер-источник §4–§7 |
| `tz.md` | ТЗ «Петровское СП» (модули, функциональные требования FR, формы 2.3) |
| `design-system-v2.html` + `assets/ds.css` | Дизайн-система v2 — визуальный канон. Правки только в `ds.css` |
| `index.html` | Карта-лаунчер прототипа (экраны и состояния) |
| `design-system.html` | Старая ДС — эталоны для переноса компонентов и экранов |

> **Примечание:** `Product.md` и `ТЗ.md` относятся к старому концепту «Сеанс» (кино-дневник) и не имеют отношения к текущему проекту. Могут быть удалены или заархивированы по вашему решению.

---

## 1. Идея продукта

Единая цифровая платформа спортивного комплекса Петровского сельского поселения: мобильное приложение (iOS + Android) для жителей, спортсменов, тренеров и судей.

Продукт мотивирует к регулярным занятиям спортом и здоровому образу жизни через:

- персональный рейтинг активности (баллы за посещения, тренировки, участие в соревнованиях);
- простую запись на тренировки любого формата (индивидуальные, групповые, самостоятельные, аренда);
- живую среду сообщества: клубы, события, лента новостей, друзья;
- умную адаптацию нагрузок на основе самочувствия (опросы после тренировок) и подготовку к сдаче норм ГТО.

**Ценность для пользователя:** «Записался — сходил — получил баллы и стал частью сообщества». Цикл повторяемый, с заметным прогрессом.

---

## 2. Пользователи и роли

| Роль | Кто | Ключевой доступ |
|---|---|---|
| **Клиент (спортсмен)** | Житель поселения, записывается на тренировки, сдаёт ГТО, участвует в соревнованиях, общается в чатах | Все модули |
| **Тренер** | Ведёт индивидуальные/групповые тренировки, консультирует, формирует планы | Расписание, консультации, сообщества |
| **Судья** | Проводит соревнования; судейство включается **по запросу** в настройках карточки соревнований | Соревнования, карточки событий |
| **Гость (неавторизованный)** | Не залогинен | Лента новостей, информация о комплексе (ограниченно) |

Дополнительно: для **несовершеннолетних** в анкете фиксируется контакт родителя / ответственного лица.

---

## 3. Сценарии и цели (JTBD)

1. **Записаться на тренировку** в пару тапов: главная → расписание → слот → подтверждение.
2. **Не пропустить тренировку**: уведомления о начале, изменениях, отменах; карточка ближайших записей на главной.
3. **Видеть свой прогресс**: рейтинг и баллы на главной (дашборд) и в личном кабинете — с расшифровкой, за что начислены.
4. **Подготовиться к ГТО**: конструктор тренировок с заготовкой упражнений под нормативы, план подготовки.
5. **Тренироваться осознанно**: опрос после тренировки → в следующий раз план подстроится под самочувствие.
6. **Быть в сообществе**: клубы по интересам, события, лента, друзья, приглашения.
7. **Попасть в клуб / на тренажёр**: QR-сканер для входа.
8. **Спланировать неделю**: календарь тренировок и записей, конструктор произвольной тренировки.

---

## 4. Механики (системные правила)

### 4.1 Аутентификация
Вход/регистрация по коду (SMS или email). Сразу после — выбор роли. Роль «судья» включается позже по запросу, а не при регистрации.

### 4.2 Рейтинг и баллы
- Баллы начисляются за активности (посещения, тренировки, соревнования).
- Из баллов формируется рейтинг: общий / по региону / по друзьям.
- Рейтинг показывается на главной как дашборд; детали — по тапу («за что начислены баллы»), полная версия — в личном кабинете.
- Правила начисления (какие активности = сколько баллов) — **открытый вопрос**, будет зафиксирован отдельно.

### 4.3 Запись на тренировки
Четыре формата, каждая со своим флоу:
| Формат | Фильтры | Шаги |
|---|---|---|
| Индивидуальная | тип тренировки, тренер | фильтр → карточка тренера со слотами → запись/отмена |
| Групповая / событие | тип, тренер, возрастная группа, сложность | фильтр → запись/отмена |
| Самостоятельная | — | (см. конструктор тренировок) |
| Аренда (корт/площадка/ролики/зал) | — | выбор слота → запись/отмена |

После завершения любой записи приходит опрос «как прошло» → в конструктор предлагаются упражнения с корректировкой интенсивности/сложности.

### 4.4 Календарь и статусы записей
- Календарь запланированных тренировок: тап по дате → карточка тренировки (инфо, информация о прибытии, состояние после тренировки, если заполнен опрос).
- Календарь записей: просмотр всех (строчный режим) со статусами **посетил / не прибыл**, добавление/изменение/отмена.
- **Правило статусов**: «посетил / не прибыл» — база для начисления баллов.

### 4.5 Конструктор тренировок
- Формирование произвольной тренировки и добавление её в календарь.
- Для ГТО — отдельная заготовка упражнений под сдачу нормативов.
- Рекомендации на основе предыдущих опросов о самочувствии.
- Добавление / отмена / изменение.

### 4.6 Опрос после тренировки
- Триггеры: после индивидуальной, после групповой, после аренды площадки.
- Пара коротких вопросов о самочувствии.
- Влияет на рекомендации конструктора при следующем планировании.

### 4.7 Лента новостей
- Фильтр по тегам: события площадки / события друзей / события клубов.
- Из новости можно перейти к мероприятию и записаться → событие появляется в календаре.

### 4.8 Клубы и сообщества
- Сообщества по интересам на главной.
- Внутри: мероприятия и соревнования сообщества, кто из друзей состоит.
- Клуб: расписание (самостоятельные / групповые / индивидуальные / аренда), быстрая запись из карточки события, шеринг ссылки на тренировку.

### 4.9 Друзья
Список друзей и их активность, пригласить друга / поделиться событием, удалить друга.

### 4.10 Уведомления
Типы: о начале тренировки, о новой записи, об отмене, о большом событии (например соревнование с участием спортшколы). Уведомление можно удалить.

### 4.11 QR-доступ
Сканер с переключателем вкладок: **вход в клуб** / **сканирование кода на тренажёре**.

### 4.12 ГТО (из ТЗ)
- План подготовки к сдаче нормативов (индивидуальный график).
- Отслеживание прогресса выполнения нормативов.
- Онлайн-консультация тренера.
- В новом флоу ГТО представлено как заготовка в конструкторе тренировок; полный модуль «Дневник ГТО» из ТЗ — **открытый вопрос** (см. §8).

### 4.13 Чат и консультации (из ТЗ)
- Чат спортивного сообщества: личные и групповые чаты (клубы, тренер), фото/видео/голос.
- Консультации тренера.
- В новом флоу эти модули не упоминаются — **открытый вопрос** (см. §8).

### 4.14 ГТО — фактическое состояние (по анализу экранов из другого проекта)

**Логически ГТО уже интегрировано, визуально — нет.**

- **Данные:** кнопка «Добавить тренировки в календарь» в плане подготовки реально закидывает 9 тренировок (3 недели × 3 дня) + тест-день в общий календарь (с тостом-подтверждением). ГТО-подготовка — это **не пятый тип события**, а обычные тренировки, появляющиеся в общем календаре.
- **Визуал и навигация разъехались:** экраны `gto.html`, `gto-plan.html`, `gto-test.html`, `gto-workout.html`, `gto-result.html` живут на своей дизайн-системе — свои токены `:root`, шрифт Oswald вместо Inter, синий `#1a86e0` вместо акцента `#3b7bff`/фиолетового градиента, не подключают общий CSS, который используют основные экраны. «Назад» в `gto.html` ведёт на старую главную.
- **Приоритет при сборке:** пересадить ГТО-экраны на ДС v2 (`assets/ds.css`) и поправить навигацию — иначе модуль будет ощущаться чужеродным.

**Три разных «финиша» тренировки:**

| Флоу | Что происходит после | Проблема |
|---|---|---|
| `training-active` → `training-report` | Обычная тренировка → полный отчёт | — |
| `gto-workout` → возврат в `gto-plan` | Тренировка по плану ГТО → **без отчёта** | Пользователь не получает фидбек (сколько сделал, сколько минут), хотя после обычной тренировки получает |
| `gto-test` → `gto-result` | Сдача норматива → фиксация результата теста | Открывается не из тренировки, а из календаря; «назад» ведёт в календарь |

Смысловое разделение удачное: **«потренироваться по плану ГТО» = обычная тренировка**, **«сдать норматив» = событие-тест со своим замером**, это не тренировка. Но тогда нужно решить: унифицировать экран отчёта на все типы тренировок (с ГТО-меткой в шапке) или осознанно отказаться от отчёта для ГТО-подготовки — см. §8, вопрос 8.

### 4.15 Тренеры — две отдельные фичи
Сейчас тренеры нигде не смоделированы, кроме заглушки: кнопка «Спросить тренера» показывает тост «откроется в полной версии». Ни карточки тренера, ни выбора слота, ни привязки тренера к клубному событию нет. В ТЗ роль «тренер» не документирована (раздел 1.4).

Две разные фичи, обе пустые:
1. **Тренер как автор события** — кто создаёт «тренировку с тренером», как выбираются слоты.
2. **Тренер как канал консультации** — чат/вопросы тренеру.

Каждую проектируем отдельно (см. §8, вопрос 10).

### 4.16 Шаблоны тренировок — единая модель
В текущих экранах шаблоны уже фактически есть, но не оформлены как сущность:
- `gto-plan.html` (норматив → группы мышц → сетка «упражнение × 7 дней × 3 недели») — шаблон программы, захардкоженный под ГТО.
- «Повторить» в журнале тренировок — разовый шаблон из фактически проведённой тренировки (ad hoc, из истории).

**Единая сущность «шаблон / план»** = { название, теги-цели (общая форма / ГТО-норматив X / произвольная), сетка упражнений по дням, флаг «сгенерировать N тренировок в календарь» }.

Тогда: конструктор — редактор одного шаблона; ГТО-план — шаблон с прошитой целью-нормативом; «Повторить» из журнала — разовый шаблон из проведённой тренировки. Один движок вместо трёх параллельных (см. §8, вопрос 9).

**Решение (зафиксировано):** конструктор/план живёт в **профиле** (раздел «План тренировок»), а не в календаре. Результат — готовая сетка тренировок — **выгружается в общий календарь** по флагу «сгенерировать N тренировок» (та же механика, что в ГТО: «Добавить тренировки в календарь» = обычные тренировки в общем календаре, §4.14). В календаре никакого UI конструктора нет — только отображение выгруженных тренировок.

### 4.17 Меню управления событием (⋯)

Кнопка «⋯» (`.ev-card .ev-menu`) — в правом верхнем углу карточки события, на одной оси с пилюлей статуса (правый отступ `--space-4`). Тап открывает нижний лист действий `.sheet-list` (ЭТАП 9 ДС v2). Канон пункта меню: иконка-тайл 40 (`.sheet-ic`) · название (`.sheet-il`) · подсказка (`.sheet-ih`).

Канонический набор действий (эталон — ДС v2, «Меню „⋯“ · .sheet-list»):

| Действие | Иконка | Подсказка | Доступность |
|---|---|---|---|
| Напомнить | `ic-bell` | Уведомление за 30 минут | всегда |
| Поделиться с клубом | `ic-users` | Отправить участникам сообщества | события клуба |
| Перенести время | `ic-clock` | Доступно за 3 часа до начала | иначе пункт `.is-off` |

- Недоступное действие — `.is-off`: 45% прозрачности, без ховера; причина указана в подсказке (напр. «Доступно за 3 часа до начала»).
- Набор пунктов контекстный: для событий, которые создал я (`.is-org`, «Админ») — управление событием (перенос, отмена, редактирование); для записей на чужие события — управление записью (отмена, напоминание). Полная матрица ниже (канон — ДС §25 «Меню „⋯“», `design-system.html`).
- Отмена — `.sheet-cancel` под списком (канон листа, ЭТАП 9).
- Кнопка: в карточке события `.ev-card .ev-menu` — отдельные точки в правом верхнем углу (квадрат 24px, без обводки, ховер — фон `--surface-2` и смена цвета); в карточке цели (`.goal-top`) — круглая 32px с рамкой.

**Матрица «роль × статус события → пункты меню»** (эталон — ДС §25):

**Приглашение (pending)**
- Принять (is-ok) · Отклонить (is-danger) · Кто идёт · Поделиться

**Подтверждено (confirmed)**
- Отказаться (is-danger) · Кто идёт · Поделиться
- Дополнения по типу: + клубный чат (клуб), + сетка и календарь (ивент), + QR-код (аренда)

**Запись открыта (open)**
- Записаться (is-ok) · Кто идёт · Поделиться
- Дополнения по типу: + клубный чат (клуб), + добавить в календарь (ивент)

**Вы организатор (org) · по типам**
- Редактировать · Управлять участниками · Пригласить (is-ok) · QR-код входа · Изменить время · Клубный чат · Поделиться · Отменить / Удалить (is-danger)
- Тренер: «Подтвердить / Отклонить» (пока «Ожидает»), «Написать тренеру», «Отменить» после подтверждения.
- Ивент: «Посмотреть сетку», «Добавить в календарь».
- Индивидуальная: «Редактировать», «Изменить / Добавить тренера», «Отменить», «Удалить».

**Нет действий** — у завершённых / отменённых событий и при роли «нет связи» (none). Пункт «Все действия» детали карточки открывает тот же лист.

### 4.18 Типы и статусы событий — категории, форматы, статусы (канон — ДС §26)

Вся активность продукта делится на **4 сущности-категории**. Событие = **категория** (цвет + иконка + поведение) + **форматы** (чипы с ролью-цветом). Цвет дорожки карточки, иконка и свайп-действия берутся только из категории.

| Категория | Ключ | Токен · цвет | Кто заводит | Описание |
|---|---|---|---|---|
| Тренировка | `training` | `--type-training` #3ab5ad | пользователь или тренер | Занятие с форматами: личная, с тренером, групповая, онлайн |
| Ивент | `event` | `--type-event` #ff5a5a | администратор СП | Мероприятия самого СП: турнир, марафон, слёт, сбор; подтип задаёт админ (рабочее название прототипа) |
| Аренда | `rental` | `--type-rental` #3b7bff | пользователь | Бронь локаций клуба (поле, зал, корт и т.п.) |
| Клуб | `club` | `--type-club` #9175e8 | сообщество | Событие секции по виду спорта: сборы, выезды, тренировки клуба |

**Форматы — атрибуты, не отдельные типы.** «Личная», «с тренером», «групповая», «онлайн» — атрибуты одной категории «Тренировка». Итого 4 типа вместо 7+ — меньше цветов, сущностей и логики.

**Карта сущностей → категория + формат:**

- Личная тренировка → Тренировка + «Личная»
- Тренировка с тренером → Тренировка + «С тренером»
- Онлайн-тренировка / консультация → Тренировка + «Онлайн»
- Групповое занятие → Тренировка + «Групповая»
- Соревнование / марафон / челлендж → Ивент
- Слёт / сбор поселения (заводит админ) → Ивент
- Аренда площадки → Аренда
- Тренировка клуба / сборы секции → Клуб
- Событие клуба → Клуб

**Легаси-ключи:** `me` и `trainer` из текущих экранов → категория `training` + формат-чип («личная» / «с тренером»). Алиас `--type-me` удалён: экраны используют `--type-training` напрямую.

**Статусы — 7 подписей** (статус отвечает на вопрос «что это для меня прямо сейчас?», на карточке всегда один):

| Статус | Ключ | Токен | Примечание |
|---|---|---|---|
| Приглашение | `pending` | `--st-pending` | любое событие, ждущее действия от меня; единственный требующий действия статус — жёлтый, пульс |
| Запись открыта | `open` | `--st-open` | можно записаться |
| Подтверждено | `confirmed` | `--st-confirmed` | я записался или принял (тренер, аренда) |
| Вы организатор | `org` | (роль, цвет жизненного цикла) | я создал/веду событие |
| Идёт | `ongoing` | `--st-ongoing` | событие началось (турнир на несколько дней, записи больше нет) |
| Завершено | `done` | `--st-done` | финальный |
| Отменено | `cancelled` | `--st-cancelled` | финальный |

Цвет — жизненный цикл (6 токенов: `--st-pending/open/confirmed/ongoing/done/cancelled`); «Вы организатор» — роль. Подпись карточки зависит от роли `org`: `pending → «Приглашение»`, `open/confirmed/ongoing/done/cancelled → «Вы организатор»`.

**Быстрые действия берутся из статуса** (согласовано с матрицей §4.17 / ДС §§24–25):

- **Приглашение** → «Принять · Отклонить» (тренеру — «Подтвердить»)
- **Запись открыта** → «Записаться»
- **Подтверждено** → «Отказаться · Кто идёт» (+ QR у аренды, + сетка у ивента)
- **Вы организатор** → «QR · Кто идёт · Редактировать · Перенести» (по типу)
- **Идёт** → «Результаты · QR» (по типу)
- **Завершено / Отменено** → без действий

---

## 5. Пользовательские сценарии (ключевые флоу)

1. **Регистрация нового пользователя:** welcome → auth (код) → выбор роли → онбординг → анкета (ФИО, возраст, ID, сообщество, контакт родителя) → главная.
2. **Повторный вход:** welcome → auth → выбор роли → главная.
3. **Запись на индивидуальную тренировку:** главная → расписание → вкладка «индивидуальная» → фильтр (тип, тренер) → карточка тренера со слотами → запись/отмена → опрос после завершения.
4. **Запись на групповую тренировку:** главная → расписание → вкладка «групповая» → фильтр (тип, тренер, возрастная группа, сложность) → запись/отмена → опрос после завершения.
5. **Аренда площадки/корта:** главная → расписание → вкладка «аренда» → запись/отмена → опрос после завершения.
6. **Самостоятельная тренировка:** главная → календарь записей → все записи → конструктор → добавить в календарь.
7. **Просмотр рейтинга:** главная → дашборд активности → детали рейтинга.
8. **Управление друзьями:** главная → профиль → друзья → список / пригласить / удалить.
9. **Запись через новость:** главная → лента → фильтр по тегам → переход к событию → запись → событие в календаре.
10. **Быстрая запись из карточки клуба:** главная → ближайшие события клуба → расписание клуба → подтверждение записи.
11. **Сканирование QR:** главная → сканер → вход в клуб / тренажёр.

---

## 6. Карта экранов

**Легенда статусов переноса:**
- **есть в старой ДС** — эталон существует в `design-system.html`, секция указана; требует адаптации под новую ДС.
- **частично** — в старой ДС есть отдельные блоки/компоненты, но экрана целиком нет.
- **новое** — экрана не было, дизайн строится с нуля.
- **в ДС v2** — используемые компоненты уже перенесены в `assets/ds.css`.

### 6.1 Вход и онбординг

| ID (флоу) | Экран | Ключевое содержимое | Статус в старой ДС |
|---|---|---|---|
| `welcome` | Приветственный экран | Бренд, фон, CTA | `splash` |
| `auth` | Вход / регистрация по коду | Код по SMS или email | `forms` (авторизация) |
| `role_selection` | Выбор роли | Клиент / Тренер (судья — по запросу) | **новое** |
| `onboarding` | Онбординг | Знакомство с продуктом | **новое** |
| `questionnaire` | Анкета | ФИО, возраст, ID, сообщество, контакт родителя | **новое** |

### 6.2 Главный экран

| ID | Экран | Ключевое содержимое | Статус в старой ДС |
|---|---|---|---|
| `home` | Главная | Дашборд: активность и рейтинг; лента новостей (фильтр по тегам); карточка ближайших записей; ближайшие события клуба (быстрая запись + кто из друзей записан); список сообществ; промо-баннер анонса (случайный вариант при обновлении) | **частично** (`header`, `points`, `cards`, `chips`) |
| `notifications` | Уведомления | 4 типа уведомлений, удаление | **частично** (`toast`, `.notif`) |

Промо-баннер главной (`screens/06-home.html`, он же `home-v2.html`): набор вариантов `BANNERS` — пастельный фон + картинка из `assets/banners/*.png` (306×306, прозрачность) + свои дата/заголовок/число участников под вид спорта. При каждом обновлении страницы выбирается случайный вариант. Пастели подобраны под доминирующий цвет арта: жёлтый `#fdebcf` · персиковый `#f0cfc5` · мятный `#c2e4d4` · лавандовый `#d2d1ef` · небесно-голубой `#b7ddf3`; фон — inline-`background` (дефолт в CSS `#fdebcf`), картинка `.promo-media` высотой 132% уходит за правый край. Старый `assets/promo-banner.png` не используется (файла нет).

### 6.3 Расписание и запись

| ID | Экран | Ключевое содержимое | Статус в старой ДС |
|---|---|---|---|
| `schedule` | Расписание платформы | 4 вкладки: индивидуальные / групповые / самостоятельные / аренда | **частично** (`tabs`, `tabbar`, `chips`) |
| `individual_training` | Запись на индивидуальную | Фильтр → карточка тренера со слотами → запись/отмена | **частично** |
| `group_training` | Запись на групповую / событие | Фильтр → запись/отмена | **частично** |
| `self_training` | Самостоятельные тренировки | Связан с конструктором | **частично** |
| `court_rental` | Аренда площадки/корта | Выбор слота → запись/отмена | `forms` (бронирование площадки) |
| `club_schedule` | Расписание клуба | Самостоятельные / групповые / индивидуальные / аренда; запись, отмена, шеринг ссылки | **частично** |

### 6.4 Календарь и тренировки

| ID | Экран | Ключевое содержимое | Статус в старой ДС |
|---|---|---|---|
| `training_calendar` | Календарь тренировок | Тап по дате → карточка тренировки | `calendar` |
| `booking_calendar` | Календарь записей | Все записи / добавить / изменить / отменить | `calendar` |
| `all_bookings_list` | Все записи (строчный режим) | Запись + статус: посетил / не прибыл | `rows` |
| `training_constructor` | Конструктор тренировки | Произвольная тренировка, заготовка ГТО, рекомендации из опросов | **новое** |
| `post_training_survey` | Опрос после тренировки | Пара вопросов о самочувствии | **новое** |
| `add_event` | Создание события | Мастер: категория → детали · аренда → 3 шага · из FAB календаря | `create-category` |

**Фильтр календаря «Все / Мои / Приглашения»** (экран `07-calendar.html`): открывается кнопкой-воронкой `ic-sliders` в шапке календарной карточки (`.cal-head`, между месяцем и стрелками навигации) как нижний лист. «Мои» — события со статусом `confirmed`/`done`, «Приглашения» — `pending`, «Все» — без фильтра. Кнопка фильтра — иконка обычного цвета с обводкой `1px solid var(--border-soft)` (как у тап-бара); активный режим (`.is-on`) — только синяя иконка, обводка та же. Шевроны навигации по месяцам — приглушённый `--meta`, при наведении `--fg-2`.

**Вертикальный ритм календаря** (`07-calendar.html`) = канон главной: чипсы-фильтр вплотную к хедеру (0px, как плашка «СП Петровское» `.cv`), блок календаря `.cal-card` через 8px (`--space-2`), заголовок панели дня `.day-head` через 24px (`--space-6`, как `.sec`-секции на главной).

**Фильтр по типу — чипы выбора `.pick-chip`** (из анкеты welcome-v2, внесены в DS): ряд `.chips.is-wrap` в 2 колонки без скролла, кнопка-переключатель с рамкой `--border`; при выборе `.is-on` — светлый акцентный фон и синий текст (`color-mix(accent 8%)`), у типа с цветом — точка `.dot` (`--c`). Основной ряд: «Все / Тренировки / События СП / Клубы / Аренда»; фильтрует и сетку дней, и панель дня.

**Подфильтр «второй этаж»** (`.chips-sub`, тоже `.pick-chip` в 2 колонки): при выборе чипа «Тренировки» под основным рядом появляется второй ряд — «Все / Личные / С тренером / Групповые» (поле `sub` события). Выбор сбрасывается на «Все» при смене родительского чипа; для остальных типов (события/клубы/аренда) второй ряд скрыт.

**Создание события** (экран `07-add-event.html`, ID `add_event`): открывается из FAB `fab-add` на календаре. Мастер «сначала тип, тип решает всё дальше» (канон ДС §30–31 → v2 §17–18). Не-аренда (тренировка/клуб) — два шага; аренда — два шага «когда и где → детали и бронь» с отдельным степпером `.steps` (2 точки).

- **Шаг 1 — категория.** Заголовок «Создание события», подсказка «Что добавляем?», 3 карточки `.pick-card` с цветом типа (`--type-training` / `--type-rental` / `--type-club`) и иконкой. Категории «Событие» в мастере нет — такие события создаёт только админ через веб-интерфейс. CTA «Далее» в прибитой плашке `.evd-bar` — disabled, пока не выбрано.
- **Шаг 2 — детали (не аренда).** Заголовок меняется на тип; сводка выбранной категории `.evd-type` (иконка + название + «Изменить» → назад на шаг 1). Поля: Название (`.input`), формат-чипы `.fchip` **только для тренировок** («Личная / С тренером / Групповая / Онлайн»), Дата и Время — строки `.row` → нижний лист с барабаном `.wheel` (число·месяц·год и часы·минуты), Напоминание — `.switch`, Место (`.input`). CTA «Далее» — активна после ввода названия. Подтверждение: тост `toast-success` → возврат на календарь.
- **Аренда · шаг 1 — когда и где.** Заголовок шага `.step-title` + подсказка `.step-note` (как в референсе): «Площадка и время» — «Выберите дату и площадку — покажем свободные слоты.». Фон — токен `var(--surface-warm)` (`#f5f7fc`, светло-голубой, централизованно в `assets/ds.css`; совпадает со старой главной). По принципу референса `referens/create-event.html` каждый логический блок обёрнут в белую плашку `.card` (`--surface` на фоне `--surface-warm`): дата, площадка, слоты+длительность (шаг 2 — резюме, инвентарь, друзья, итог). Дата — кнопка-поле `.input` с выбранной датой: по клику открывает лист-барабан `#date-sheet` — тот же механизм, что у строки даты в «Деталях» и кнопки «Другая дата» (выбор даты единообразно барабаном). Список площадок `.facility-group` (скроллбар — тонкий, лёгкий: `6px`, `--border-strong`, прозрачный трек, как в референсе): `.facility-opt` (имя, `цена/ч · вместимость` — «1 500 ₽/ч · до 24 чел.»; адрес только в листе «О площадке») + строка доступности `.fo-avail` — «Свободно: 13:00, 14:00, 15:00 …» (синим `var(--accent)`, тем же размером, что строка цены) или «Нет мест» (карточка приглушена `.is-empty`); `.fo-info` «о площадке» → лист `.fi-list` (стоимость, вместимость, описание — доступно всегда). Под выбранной — **только свободные слоты** `.slot-grid` `.slot` (`is-free`, выбранный старт `is-sel`); слоты — в стиле чипа выбора `.pick-chip` (рамка `--border-strong`, фон `--surface` — белый по умолчанию, hover — рамка `--meta`, без заливки), выбранный — светлый акцентный фон `color-mix(accent 8%)` + синий текст; занятые и прошедшие часы не показываем, легенды нет. Длительность — те же кнопки `.slot` «1 ч · 2 ч · 3 ч» (по умолчанию 2 ч), что и слоты времени (одна и та же сущность), отделены от слотов лёгким разделителем `.divider.is-thin`; перед рядом подпись `.slot-sub` «Длительность» (заголовок как над слотами); недоступные варианты отключены `.is-off` и перечёркнуты (line-through), при смене слота выбранная длительность автоматически ужимается до доступной. Диапазон под слотом не дублируем — выбранный слот и так виден, а диапазон показывается в сводке брони в плашке CTA. Выбранный `.is-sel` — светлый синий на белом (`color-mix(accent 8%, --surface)`). Если у площадки на дату мест нет — пустая сетка + примечание `.slot-note`: надпись «На эту дату мест нет» красным (`--danger`) и ghost-кнопка `btn-ghost` «Другая дата» (иконка календаря) во всю ширину блока по центру, обводка `--border-strong` (как у чипсов слотов); без серой плашки: текст `--muted` прямо на белой карточке). Акцентный CTA на странице один — «Далее»/«Забронировать» в `.evd-bar`, вспомогательные действия только `btn-ghost`. CTA «Далее» — активна после выбора площадки и слота.
- **Аренда · шаг 2 — детали и бронь.** Заголовок шага «Дополнительно» + подсказка «Необязательно — всё можно изменить позже.». Резюме `.ext-sum`: имя площадки `.ext-name` крупно; вторая строка `.ext-line` — `.ext-when` «13 авг · 13:00 – 15:00» с вертикальным разделителем `border-right` (как колонка `when` у карточки события на главной) и итоговая цена `.ext-cost` **чёрным** (`--fg`) после разделителя; третья `.ext-sub` «1 500 ₽ / ч · до 24 чел.» (цена за час + вместимость, стиль строки цены площадки); кнопка «Изменить» — в углу `.ext-sum-edit` (→ назад на шаг 1); слева цветная полоса `--type-rental`. Переключатели — строки `.card-row` с `.card-name` + `.card-sub` (без серых иконок-тайлов, светло как в референсе): «Инвентарь» / «Подберём под площадку — мячи, ракетки, коврики» (select `.input` с набором под площадку + стоимость `.cost-line`), «Позвать друзей» / «Идёте не один? Соберите компанию» (пикер людей — нижний лист `.sheet` с поиском `.sheet-search`, списком `.sheet-item`, счётчиком `.counter` «2 чел.», кнопка «Добавить»). Итог `.total-rows` строками `.cost-row`: «Площадка / Инвентарь / К оплате». CTA «Забронировать» → тост «Бронь оформлена» → возврат на календарь через ~1.4 с.
- **Плашка `.evd-bar` (аренда):** шаг 1 — сводка брони `.recap` (grid «иконка | название / дата · время | цена»: вторая строка — дата · время-диапазон, справа цена `.recap-cost` чёрной (`--fg`) по центру за длинным вертикальным разделителем `border-left` на высоту двух строк, как колонка `when` карточки события на главной) + ряд `.bar-actions` «Назад» + «Далее»; шаг 2 — без recap (резюме уже сверху), «Назад» + «Забронировать». Степпер `.steps` — 2 точки под топбаром только для аренды; активная и пройденные точки — фирменный градиент `linear-gradient(135deg, var(--accent), var(--accent-purple))`.
- **Навигация:** крестик / назад с шага 1 → `07-calendar.html`; «Назад» — по шагам аренды, с шага 1 аренды → к выбору типа.


### 6.5 Сообщество и события

| ID | Экран | Ключевое содержимое | Статус в старой ДС |
|---|---|---|---|
| `news_feed` | Лента новостей | Фильтр по тегам, переход к событию | `cards`, `chips` |
| `communities` | Список сообществ | Сообщества по интересам на главной | **частично** (`org-card`, `complex`) |
| `community_details` | Детали сообщества | Мероприятия и соревнования, кто из друзей состоит | **частично** |

### 6.6 Профиль и сервис

| ID | Экран | Ключевое содержимое | Статус в старой ДС |
|---|---|---|---|
| `profile` | Личный кабинет | ФИО, возраст, ID, сообщество, контакт родителя; рейтинг активности; роль и статус; друзья; настройки | `profile` |
| `rating_details` | Детали рейтинга | За что начислены баллы и сформирован рейтинг | `points`, `stats` |
| `friends` | Друзья | Список и активность, пригласить, удалить | **частично** |
| `settings` | Настройки | Уведомления, аккаунт, приватность, удаление, загрузка фото | **частично** |
| `qr_scanner` | Сканирование QR | Вход в клуб / код на тренажёре | `scanner` |

---

## 7. Навигация

Исходный набор из флоу (`home.navigation`): **Расписание · Календарь тренировок · Уведомления · Профиль · QR-сканер**.

Состав нижнего таб-бара и размещение QR-сканера — **открытый вопрос** (см. §8): типичное решение — таб-бар из 4–5 пунктов плюс QR как кнопка-иконка на главной или в шапке профиля.

---

## 8. Открытые вопросы

1. **Состав нижней навигации:** какие пункты в таб-бар, куда поставить QR-сканер (таб / иконка в шапке / главная).
2. **ГТО как модуль:** оставить «Дневник ГТО» отдельным модулем (план подготовки, прогресс нормативов, консультации тренера) или достаточно заготовки в конструкторе тренировок?
3. **Чат и консультации тренера:** переносим ли модули из ТЗ (чат сообщества, консультации) в актуальный флоу — в новой архитектуре их нет.
4. **Роль судьи:** как запрашивается (настройки карточки соревнований), что меняется в интерфейсе.
5. **Правила начисления баллов:** какие активности и сколько баллов; алгоритм рейтинга (регион / друзья / клубы).
6. **Существующие экраны из другого проекта:** полный перечень страниц для переноса и их маппинг на карту §6.
7. **Дневник посещаемости и награды/соревнования из ТЗ:** как отражаются в новом флоу (через рейтинг и календарь или отдельными разделами).
8. **Экран отчёта по всем типам тренировок:** унифицировать (обычная, ГТО-подготовка — с меткой в шапке) или осознанно отказаться от отчёта для ГТО-подготовки и зафиксировать почему. От текущего решения зависит `gto-workout` → отчёт или возврат в план.
9. **Единая модель шаблонов/планов (§4.16):** подтвердить, что конструктор = редактор шаблона, ГТО-план = шаблон с целью-нормативом, «Повторить» = разовый шаблон из истории.
10. **Тренеры (§4.15):** две фичи (автор события + канал консультации), обе требуют дизайна с нуля; роль «тренер» зафиксировать в ролевой модели (§2).
11. **Календарь и фильтрация:** файл `schedule-calendar.html` (логика фильтров/группировки) не был доступен для анализа — зафиксировать отдельно, как только появится.
12. **Меню управления событием (§4.17):** маппинг «роль / статус события → набор пунктов меню» зафиксирован (матрица в §4.17, канон — ДС §25). Открыто: флоу действий для своих событий (перенос, отмена, редактирование — отдельные экраны или всё через ⋯).

---

## 9. Чек-лист переноса экранов в ДС v2

Заполняется по мере переноса. Компоненты переносим в `assets/ds.css`, эталонные экраны — в `design-system-v2.html` (секция 12 «Экранные блоки»).

- [ ] Приветственный экран (welcome)
- [ ] Вход / регистрация по коду (auth)
- [ ] Выбор роли (role_selection)
- [ ] Онбординг
- [ ] Анкета (questionnaire)
- [ ] Главная (home, дашборд + лента + записи + клубы + сообщества)
- [ ] Уведомления
- [ ] Расписание и 4 вкладки записи
- [ ] Расписание клуба
- [ ] Календарь тренировок
- [ ] Календарь записей + все записи
- [ ] Меню управления событием (⋯ → `.sheet-list`, контекстные действия §4.17)
- [ ] Конструктор тренировок
- [ ] Опрос после тренировки
- [ ] ГТО-экраны (план, тренировка, тест, результат) — пересадить с собственной ДС на `ds.css`, поправить «Назад»
- [ ] Лента новостей
- [ ] Сообщества и детали сообщества
- [ ] Профиль (данные, рейтинг, роль, друзья, настройки)
- [ ] Тренер: карточка, слоты, «спросить тренера» (после решения §8.10)
- [ ] QR-сканер
